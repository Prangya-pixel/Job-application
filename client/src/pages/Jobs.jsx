import {useEffect,useState} from 'react';import {getJobs} from '../api/jobs';import JobCard from '../components/JobCard';import Loader from '../components/Loader';export default function Jobs(){const [jobs,setJobs]=useState([]),[filters,setFilters]=useState({search:'',location:'',jobType:'',experienceLevel:''}),[loading,setLoading]=useState(true);const load=()=>{setLoading(true);getJobs(filters).then(r=>setJobs(r.data)).finally(()=>setLoading(false))};useEffect(load,[]);return <section><p className="eyebrow">Opportunities</p><h1>Find your next role</h1><form className="filters" onSubmit={e=>{e.preventDefault();load()}}><input placeholder="Job title, skill, or company" value={filters.search} onChange={e=>setFilters({...filters,search:e.target.value})}/><input placeholder="Location" value={filters.location} onChange={e=>setFilters({...filters,location:e.target.value})}/><select value={filters.jobType} onChange={e=>setFilters({...filters,jobType:e.target.value})}><option value="">Any type</option>{['Full-Time','Part-Time','Internship','Contract','Remote'].map(x=><option key={x}>{x}</option>)}</select><select value={filters.experienceLevel} onChange={e=>setFilters({...filters,experienceLevel:e.target.value})}><option value="">Any level</option>{['Entry','Mid','Senior','Lead'].map(x=><option key={x}>{x}</option>)}</select><button className="button">Search</button></form>{loading?<Loader/>:<div className="jobs-grid">{jobs.length?jobs.map(j=><JobCard key={j._id} job={j}/>):<p>No roles match those filters.</p>}</div>}</section>}
