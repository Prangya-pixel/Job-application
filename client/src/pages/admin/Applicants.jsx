import {useEffect,useState} from 'react';import {applicant,applicants} from '../../api/applications';import Loader from '../../components/Loader';export default function Applicants(){const [items,setItems]=useState(),[search,setSearch]=useState(''),[selected,setSelected]=useState(null);const load=()=>{setItems(null);applicants({search}).then(r=>setItems(r.data))};useEffect(load,[]);if(!items)return <Loader/>;return <section><p className="eyebrow">People</p><h1>Applicant records</h1><div className="filters compact"><input placeholder="Search name or email" value={search} onChange={e=>setSearch(e.target.value)}/><button className="button" onClick={load}>Search</button></div><div className="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Joined</th></tr></thead><tbody>{items.map(u=><tr key={u._id}><td><button className="text-button" onClick={()=>applicant(u._id).then(r=>setSelected(r.data))}><b>{u.name}</b></button></td><td>{u.email}</td><td>{u.phone}</td><td>{new Date(u.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div>{selected&&<div className="admin-note"><button className="text-button" onClick={()=>setSelected(null)}>Close ×</button><h3>{selected.user.name}'s application history</h3>{selected.applications.length?<ul>{selected.applications.map(a=><li key={a._id}>{a.job?.title||'Removed job'} at {a.job?.company||'—'} — <b>{a.status}</b></li>)}</ul>:<p>No applications submitted.</p>}</div>}</section>}
