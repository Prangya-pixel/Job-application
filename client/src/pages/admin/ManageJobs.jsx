import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {changeJobStatus,deleteJob,getJobs} from '../../api/jobs';import Loader from '../../components/Loader';export default function ManageJobs(){const [jobs,setJobs]=useState();const load=()=>getJobs({all:true}).then(r=>setJobs(r.data));useEffect(load,[]);if(!jobs)return <Loader/>;const remove=async id=>{if(confirm('Delete this job?')){await deleteJob(id);load()}};return <section><div className="section-title"><div><p className="eyebrow">Job management</p><h1>Open positions</h1></div><Link className="button" to="/admin/jobs/new">Post a job</Link></div><div className="table-wrap"><table><thead><tr><th>Role</th><th>Location</th><th>Type</th><th>Status</th><th>Actions</th></tr></thead><tbody>{jobs.map(j=><tr key={j._id}><td><b>{j.title}</b><br/><small>{j.company}</small></td><td>{j.location}</td><td>{j.jobType}</td><td><button className="status-button" onClick={async()=>{await changeJobStatus(j._id,j.status==='Active'?'Inactive':'Active');load()}}>{j.status}</button></td><td><Link to={`/admin/jobs/${j._id}/edit`}>Edit</Link> <button className="danger text-button" onClick={()=>remove(j._id)}>Delete</button></td></tr>)}</tbody></table></div></section>}
