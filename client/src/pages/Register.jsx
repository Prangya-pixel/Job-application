import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';export default function Register(){const [form,setForm]=useState({name:'',email:'',phone:'',password:''}),[error,setError]=useState('');const {register}=useAuth(),nav=useNavigate();const submit=async e=>{e.preventDefault();setError('');try{await register(form);nav('/applicant/jobs')}catch(e){setError(e.response?.data?.message||'Unable to create account')}};return <section className="auth"><form onSubmit={submit} className="form-card"><p className="eyebrow">Your next chapter</p><h1>Create your account</h1>{error&&<p className="error">{error}</p>}{[['name','Full name','text'],['email','Email','email'],['phone','Phone number (optional)','tel'],['password','Password (6+ characters)','password']].map(([k,l,t])=><label key={k}>{l}<input type={t} required={k!=='phone'} minLength={k==='password'?6:undefined} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button className="button">Create account</button><p>Already registered? <Link to="/login">Sign in</Link></p></form></section>}
