import {Link,NavLink} from 'react-router-dom';import {useAuth} from '../context/AuthContext';export default function Navbar(){const {user,logout}=useAuth();return <header><Link className="brand" to="/">Career<span>Flow</span></Link><nav><NavLink to="/jobs">Find jobs</NavLink>{user?.role==='applicant'&&<NavLink to="/applicant/applications">My applications</NavLink>}{user?.role==='admin'&&<NavLink to="/admin/dashboard">Admin dashboard</NavLink>}{user?<button className="text-button" onClick={logout}>Sign out</button>:<><NavLink to="/login">Sign in</NavLink><Link className="button small" to="/register">Get started</Link></>}</nav></header>}
