import {Link} from 'react-router-dom';export default function JobCard({job}){return <article className="job-card"><div className="company-icon">{job.company[0]}</div><div><p className="eyebrow">{job.company}</p><h3>{job.title}</h3><p className="muted">{job.location} · {job.jobType} · {job.experienceLevel}</p></div><div className="job-footer"><span className="tag">{job.salary||'Competitive salary'}</span><Link to={`/jobs/${job._id}`} className="arrow">View role →</Link></div></article>}
