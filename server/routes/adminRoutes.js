import {Router} from 'express';import * as c from '../controllers/adminController.js';import {protect} from '../middleware/authMiddleware.js';import {authorize} from '../middleware/roleMiddleware.js';const r=Router();r.use(protect,authorize('admin'));r.get('/dashboard',c.dashboard);r.get('/applications',c.applications);r.patch('/applications/:id/status',c.updateApplicationStatus);r.get('/applicants',c.applicants);r.get('/applicants/:id',c.applicant);export default r;
