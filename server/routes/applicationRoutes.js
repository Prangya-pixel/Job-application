import {Router} from 'express';import * as c from '../controllers/applicationController.js';import {protect} from '../middleware/authMiddleware.js';import {authorize} from '../middleware/roleMiddleware.js';const r=Router();r.use(protect);r.post('/',authorize('applicant'),c.createApplication);r.get('/my-applications',authorize('applicant'),c.myApplications);r.get('/:id',c.getApplication);export default r;
